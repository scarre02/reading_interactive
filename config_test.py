# WARNING: Configuration file used exclusively for testing purposes
# Do NOT use these in production environments.

# Test database credentials and connection parameters
db_config = {
    'host': 'localhost',
    'user': 'test_user',
    'password': 'test_pass',
    'database': 'test_db'
}

# These values should not send real emails; they may be used with a mock SMTP server
EMAIL_USER = 'test@example.com'
EMAIL_PASS = 'test_password'
